# Ralph Progress Log
Started: Wed Mar  4 01:15:11     2026
---

## Codebase Patterns
- Dashboard is a Vite SPA with pages in `dashboard/src/pages/` and components in `dashboard/src/components/`
- Router is in `dashboard/src/router.tsx` using react-router-dom v7
- Pre-existing TS errors in `server/routes/archives.ts` and `server/routes/projects.ts` (string | string[] type issues) — not introduced by this work
- Sidebar nav items are defined in `dashboard/src/components/sidebar.tsx` as a `navItems` array
- Project uses Tailwind v4 with CSS variables in `dashboard/src/globals.css`
- WebSocket provider wraps dashboard layout in router.tsx
- Dashboard source is at `dashboard/` from repo root (NOT under `scripts/ralph/dashboard/`)
- Design system reference is at `design-system/MASTER.md` from repo root
- RalphStatusIndicator supports `compact` prop for icon-only display
- Server log cache module is at `dashboard/server/log-cache.ts` — tracks current Story ID and caches logs by story
- Express v5 `req.params` returns `string | string[]` — use `as string` cast for single params
- WebSocket `ralph:output` events now include `storyId` field (string | null)
- Git-specific API routes are under `/api/git/*` in `dashboard/server/routes/git.ts`

---

## 2026-03-04 - US-001
- Deleted terminal.tsx, stories.tsx, logs.tsx, archives.tsx pages
- Deleted terminal-view.tsx component
- Removed @xterm/xterm, @xterm/addon-fit, @xterm/addon-web-links from package.json
- Updated router.tsx: removed page routes, added redirect routes for old paths to /dashboard
- Ran npm install to update lock file
- Verified no remaining references to deleted files
- TypeScript check passes (only pre-existing server errors remain)
- Files changed: src/pages/terminal.tsx, src/pages/stories.tsx, src/pages/logs.tsx, src/pages/archives.tsx, src/components/terminal-view.tsx (deleted), src/router.tsx, package.json, package-lock.json
- **Learnings for future iterations:**
  - The sidebar.tsx still has nav items for terminal/stories/logs/archives (links only, not imports) — US-002 will clean those up
  - Pre-existing TS errors exist in server routes — don't be alarmed by them
  - xterm dependencies had missing type declarations even before removal (types weren't installed)
---

## 2026-03-04 - US-002
- Rewrote sidebar.tsx as 64px icon rail with Claude light theme
- Removed Terminal, Stories, Logs, Archives nav items and icons
- Sidebar: #EDEDEA background, 1px #E0DDD5 right border
- Logo: 40x40 rounded square, #C15F3C background, white 'R' in Cormorant Garamond serif
- Active nav: #C15F3C background with white icon; Inactive: transparent with #999999 icon
- Bottom: Settings icon + Ralph status dot (10x10 green circle)
- Added `compact` prop to RalphStatusIndicator for icon rail mode (dot only, no text)
- Removed ProjectSwitcher from sidebar (moved to dashboard header area)
- Removed Sheet/mobile drawer dependencies from sidebar, replaced with simple mobile top bar
- Router already has redirect routes for old paths → /dashboard (from US-001)
- TypeScript check passes (only pre-existing server errors remain)
- Files changed: dashboard/src/components/sidebar.tsx, dashboard/src/components/ralph-status-indicator.tsx
- **Learnings for future iterations:**
  - The sidebar was completely rewritten, not just modified — old dark theme classes all replaced
  - RalphStatusIndicator now accepts `compact` prop for different display modes
  - ProjectSwitcher component still exists but is no longer in sidebar — may be used in dashboard header
  - Design system MASTER.md is at `design-system/MASTER.md` (not under scripts/ralph/dashboard)
  - Dashboard source is at `dashboard/` from repo root, not `scripts/ralph/dashboard/`
---

## 2026-03-04 - US-003
- Created `dashboard/server/log-cache.ts` — in-memory log cache with Map<string, string[]> keyed by Story ID
- Log cache parses progress.txt to determine current Story ID, also detects Story ID changes from output text
- Modified `dashboard/server/index.ts` to intercept `ralph:output` events: attaches `storyId` field to payload and caches log lines
- Log cache clears on ralph process start (fresh run)
- Added `GET /api/logs` route returning all execution logs
- Added `GET /api/logs/:storyId` route returning logs filtered by Story ID
- Existing `/api/logs/progress` and `/api/logs/git` routes preserved
- TypeScript check passes (only pre-existing server errors remain)
- Files changed: `dashboard/server/log-cache.ts` (new), `dashboard/server/index.ts`, `dashboard/server/routes/logs.ts`
- **Learnings for future iterations:**
  - Express v5 route params are typed as `string | string[]`, need `as string` cast
  - Route order matters: specific routes (`/progress`, `/git`) must come before param routes (`/:storyId`)
  - The `onEvent` callback in index.ts is the single point to intercept all Ralph process events before broadcasting
  - Log cache is in-memory only — clears on server restart (by design)
---

## 2026-03-04 - US-004
- Created `dashboard/server/routes/git.ts` with `GET /api/git/commits?storyId=US-XXX` endpoint
- Uses simple-git `git.log({ "--grep": storyId })` to filter commits by Story ID in commit message
- Returns array of `{ hash, message, author, date }` objects; empty array when no matches
- Registered gitRouter at `/api/git` in `dashboard/server/index.ts`
- TypeScript check passes (only pre-existing server errors remain)
- Files changed: `dashboard/server/routes/git.ts` (new), `dashboard/server/index.ts`
- **Learnings for future iterations:**
  - simple-git `log()` accepts `"--grep"` option to filter commits by message pattern
  - The existing `/api/logs/git` endpoint in logs.ts fetches general git history; the new `/api/git/commits` is for story-specific filtering
  - Git routes are now separate from log routes — `/api/git/*` vs `/api/logs/*`
---

## 2026-03-04 - US-005
- Implemented enhanced Story detail drawer component (story-detail-panel.tsx)
- Full rewrite: Sheet-based right-side drawer, 560px width, white bg with warm shadow
- Header: Story ID (JetBrains Mono 13px bold terracotta), status badge (pill style), close button, title (Cormorant Garamond 20px 600)
- 4 tabs: 详情, 日志, Git, 时间线 — last 3 with placeholder content for future stories
- Claude-themed tab styling via `.tab-trigger-claude` CSS class: active #C15F3C with 2px bottom border, inactive #B1ADA1
- Details tab: description, acceptance criteria (checkbox style with green/unchecked states), priority badge, notes
- Edit mode: toggle button, inline editing for title/description/notes/criteria, save via PUT /api/prd/stories/:id
- story-flow.tsx already wired with onNodeClick → setSelectedStory → setPanelOpen
- Added tab-trigger-claude CSS rules to globals.css
- Files changed: `dashboard/src/components/flow/story-detail-panel.tsx`, `dashboard/src/globals.css`
- **Learnings for future iterations:**
  - The Sheet component from shadcn/ui accepts `showCloseButton={false}` to suppress default close button (custom close button used instead)
  - Tab styling override uses `variant="line"` on TabsList plus custom CSS class `.tab-trigger-claude` for Claude theme colors
  - The `after` pseudo-element from the base Tabs component provides underline on active state — custom CSS overrides its color to terracotta
  - Edit state uses PUT /api/prd/stories/:id endpoint (not PATCH /api/prd) — this endpoint exists in prd.ts routes
---
