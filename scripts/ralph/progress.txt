# Ralph Progress Log
Started: Tue Mar  3 11:21:16     2026
---

## Codebase Patterns
- Story type is in `dashboard/src/lib/types.ts`, used by all API routes and components
- PRD read/write uses atomic file operations (tmp + rename) in `dashboard/src/lib/prd-file.ts`
- API routes at `dashboard/src/app/api/prd/stories/[id]/route.ts` handle story CRUD
- WebSocket server at `dashboard/server/ws.ts` broadcasts file change events
- Dashboard uses zustand store at `dashboard/src/lib/store.ts` for state management
- Dark theme enforced globally: zinc-900 bg, zinc-800 borders, cyan/blue accents
- Story status determined by: `passes=true` → completed, `id === currentStoryId && ralphRunning` → running, else pending
- shadcn/ui components in `dashboard/src/components/ui/`
- Flow components (ReactFlow-based) live in `dashboard/src/components/flow/`
- @xyflow/react styles imported globally in globals.css; ReactFlow components need `"use client"`
- ReactFlow custom nodes: wrap with `memo()`, export data interface, use `Handle` for connections
- TypeScript check: `cd dashboard && npx tsc --noEmit`

---

## 2026-03-03 - US-001
- What was implemented: Extended Story type with `startedAt?: string`, `completedAt?: string`, and `status?: StoryStatus` fields. Updated PUT API route to auto-set timestamps on status transitions (running→startedAt, completed/failed→completedAt, passes=true→completedAt+status).
- Files changed:
  - dashboard/src/lib/types.ts (added StoryStatus type and optional fields to Story)
  - dashboard/src/app/api/prd/stories/[id]/route.ts (auto-timestamp logic in PUT handler)
- **Learnings for future iterations:**
  - New optional fields are backward-compatible: `readPrd()` uses `JSON.parse` which ignores missing optional fields
  - The PUT route explicitly picks known fields to write, so new fields must be added there when extending Story
  - `StoryStatus` is exported from types.ts for reuse in other components
---

## 2026-03-03 - US-002
- What was implemented: Installed @xyflow/react dependency, imported its styles in globals.css, and created a minimal test component at `dashboard/src/components/flow/react-flow-test.tsx` to verify ReactFlow renders correctly.
- Files changed:
  - dashboard/package.json (added @xyflow/react dependency)
  - dashboard/package-lock.json (lockfile updated)
  - dashboard/src/app/globals.css (added `@import "@xyflow/react/dist/style.css"`)
  - dashboard/src/components/flow/react-flow-test.tsx (new test component)
- **Learnings for future iterations:**
  - @xyflow/react styles must be imported globally via `@import "@xyflow/react/dist/style.css"` in globals.css
  - ReactFlow components require `"use client"` directive in Next.js App Router
  - The `flow/` directory under components is where all flow-related components should live
  - ReactFlow container needs explicit width/height to render properly
---

## 2026-03-03 - US-003
- What was implemented: Created `dashboard/src/components/flow/start-node.tsx` — a ReactFlow custom node component displaying project overview at the top of the flow chart. Includes project name, description (truncated to 2 lines), circular progress indicator, Ralph status badge (idle/running/completed/error with iteration info), and branch name.
- Files changed:
  - dashboard/src/components/flow/start-node.tsx (new component)
- **Learnings for future iterations:**
  - ReactFlow custom nodes use `memo()` wrapping and export typed `NodeProps` with custom data interfaces
  - Use `Handle` from `@xyflow/react` with `Position.Bottom`/`Position.Top` for connection points
  - The `statusBadge` mapping and `CircularProgress` pattern from dashboard page.tsx are reused — keep them consistent
  - Custom node components need `"use client"` directive
  - SVG gradient IDs must be unique across nodes (used `startNodeProgressGradient` to avoid conflicts)
---

## 2026-03-03 - US-004
- What was implemented: Created `dashboard/src/components/flow/story-node.tsx` — a ReactFlow custom node for individual User Stories. Displays Story ID, title, status icon with color-coded borders (pending=zinc-800, running=cyan-500+pulse-glow, completed=green-500+check, failed=red-500+X). Running nodes show a live timer; completed nodes show elapsed duration. Added `pulse-glow` CSS keyframe animation in globals.css.
- Files changed:
  - dashboard/src/components/flow/story-node.tsx (new component)
  - dashboard/src/app/globals.css (added @keyframes pulse-glow and .animate-pulse-glow)
- **Learnings for future iterations:**
  - Duration formatting helper `formatDuration` uses simple threshold: <60s shows seconds only, >=60s shows Xm Ys
  - Running timer uses `setInterval` at 1s with cleanup in `useEffect` — standard pattern for real-time counters
  - `tabular-nums` class ensures timer digits don't shift layout during updates
  - Node width is fixed at 280px per PRD spec; has both top (target) and bottom (source) Handles
---

## 2026-03-03 - US-005
- What was implemented: Created `dashboard/src/components/flow/end-node.tsx` — a ReactFlow custom node for the flow chart bottom. Shows green check + "全部完成" + total duration when all stories complete; shows grey dashed border + "待完成" + remaining count otherwise.
- Files changed:
  - dashboard/src/components/flow/end-node.tsx (new component)
- **Learnings for future iterations:**
  - EndNode only has a top Handle (target) — it's the terminal node in the flow
  - Total duration calculated from first startedAt to last completedAt across all stories
  - Duration formatter extended for hours (h/m format) since total project time can exceed 60 minutes
  - Dashed border style: `border-dashed border-zinc-700` for incomplete state
---
