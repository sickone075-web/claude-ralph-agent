# Ralph Progress Log
Started: 2026-03-02

## Codebase Patterns
- Next.js 16 App Router with Tailwind CSS v4 + shadcn/ui in `dashboard/` directory
- Dark theme enforced via `className="dark"` on `<html>` element
- Sidebar layout uses `(dashboard)` route group with shared layout.tsx
- shadcn/ui components installed via `npx shadcn@latest add <component>`
- Sidebar colors: zinc-950 background, zinc-800 selected item, zinc-400 text
- All pages are at `app/(dashboard)/*/page.tsx`
- Root page (`/`) redirects to `/dashboard`
- API routes go in `app/api/` directory using Next.js Route Handlers
- API responses use `{ data: T | null, error: string | null }` format
- Config helper in `lib/config.ts` reads optional `dashboard/config.json` with defaults
- Shared types in `lib/types.ts` (PRD, Story, ApiResponse)
- PRD file read/write helper in `lib/prd-file.ts` with atomic writes and in-memory mutex
- Write operations: write to .tmp file then rename for atomic updates
- Progress log parser in `lib/progress-parser.ts` splits by `---` separator and extracts structured records
- simple-git library for git history access; project root is `path.resolve(process.cwd(), "..")`
- Ralph process manager singleton in `lib/ralph-process.ts` with event listener pattern for WebSocket integration
- Process control API routes under `app/api/ralph/` (start, stop, stdin, status)
- On Windows, use `bash` to spawn `.sh` scripts; on Unix, spawn script directly
- Process termination: Windows uses `taskkill /pid /t /f`, Unix uses SIGTERM
- WebSocket server in `server/ws.ts` runs standalone on port 3001 via `tsx`, imports ralph-process for event piping
- `npm run dev` uses `concurrently` to run Next.js + WebSocket server together
- chokidar watches prd.json and progress.txt for file change events broadcast via WebSocket
- KanbanBoard component (`components/kanban-board.tsx`) accepts `readOnly` prop for reuse in archives
- StoryEditDialog component (`components/story-edit-dialog.tsx`) is reusable for story editing
- @hello-pangea/dnd for drag-and-drop: DragDropContext > Droppable > Draggable; requires "use client"
- StorySheet component (`components/story-sheet.tsx`) for Sheet-based story create/edit forms
- StoryEditDialog component (`components/story-edit-dialog.tsx`) for Dialog-based quick story editing
- AlertDialog from shadcn/ui for delete confirmations
---

## 2026-03-02 - US-001
- Scaffolded Next.js 16 App Router project in dashboard/ directory
- Configured Tailwind CSS v4 + shadcn/ui with dark theme as default
- Created sidebar layout (240px fixed) with navigation: Dashboard, Stories, Terminal, Logs, Archives, Settings
- Ralph status indicator component (static placeholder, idle state with gray dot)
- Responsive layout: sidebar collapses to hamburger Sheet on mobile (md breakpoint)
- All nav items link to placeholder pages at app/(dashboard)/*/page.tsx
- Build succeeds with zero TypeScript errors
- Files changed: 32 files (full scaffold)
- **Learnings for future iterations:**
  - Next.js 16 uses Tailwind CSS v4 with `@import "tailwindcss"` syntax, not v3 config files
  - shadcn/ui init with `--defaults` flag auto-configures everything
  - Sheet component from shadcn/ui works well for mobile sidebar
  - Use `(dashboard)` route group to share sidebar layout across all pages without affecting URL
  - lucide-react is included as a transitive dependency from shadcn/ui
---

## 2026-03-02 - US-002
- Created shared TypeScript types in `lib/types.ts`: PRD, Story, ApiResponse<T>
- Created config helper in `lib/config.ts` with defaults and optional config.json override
- GET /api/prd returns full prd.json content
- GET /api/prd/stories returns userStories array
- GET /api/prd/stories/[id] returns single story by id
- File not found returns 404 with proper error format
- All API responses use `{ data: T | null, error: string | null }` format
- Build and typecheck pass with zero errors
- Files changed: `src/lib/types.ts`, `src/lib/config.ts`, `src/app/api/prd/route.ts`, `src/app/api/prd/stories/route.ts`, `src/app/api/prd/stories/[id]/route.ts`
- **Learnings for future iterations:**
  - Next.js 16 Route Handlers: dynamic params are `{ params: Promise<{ id: string }> }` (must await)
  - Use `satisfies ApiResponse<T>` for type-safe response bodies
  - `path.resolve(process.cwd(), "..")` navigates from dashboard/ to project root
  - Config file is optional: catch ENOENT and return defaults
---

## 2026-03-02 - US-003
- Created `lib/prd-file.ts` helper with `readPrd()`, `writePrd()`, `getNextStoryId()` functions
- Atomic file writes: write to .tmp then rename to prevent corruption
- In-memory mutex to prevent concurrent write race conditions
- POST /api/prd/stories creates new story with auto-incremented US-XXX ID
- PUT /api/prd/stories/[id] updates any story fields (title, description, acceptanceCriteria, priority, passes, notes)
- DELETE /api/prd/stories/[id] deletes a story
- PATCH /api/prd updates project-level fields (project, branchName, description)
- PATCH /api/prd/stories/reorder batch-updates priority order from array of {id, priority}
- Input validation: title required on POST, returns 400
- Refactored existing GET routes to use shared `readPrd()` helper
- Build and typecheck pass with zero errors
- Files changed: `src/lib/prd-file.ts` (new), `src/app/api/prd/route.ts`, `src/app/api/prd/stories/route.ts`, `src/app/api/prd/stories/[id]/route.ts`, `src/app/api/prd/stories/reorder/route.ts` (new)
- **Learnings for future iterations:**
  - Extract shared file read/write logic into a helper module to keep route handlers clean
  - `fs.renameSync` is atomic on same filesystem - good for preventing partial writes
  - Next.js route files can export multiple HTTP methods (GET, POST, PUT, DELETE, PATCH) from one file
  - Reorder endpoint uses PATCH on a sub-route `/stories/reorder` to avoid conflict with `/stories/[id]`
---

## 2026-03-02 - US-004
- Created `lib/progress-parser.ts` to parse progress.txt into structured ProgressRecord array
- Parser splits by `---` separator, extracts date/storyId from `## Date - US-XXX` headers
- Extracts summary bullets, files changed (from backtick-wrapped paths), and learnings section
- Added ProgressRecord and GitCommit types to `lib/types.ts`
- GET /api/logs/progress returns parsed progress records; file not found returns empty array (not error)
- GET /api/logs/git returns last 50 git commits via simple-git (short hash, message, date, author)
- Build and typecheck pass with zero errors
- Files changed: `src/lib/types.ts`, `src/lib/progress-parser.ts` (new), `src/app/api/logs/progress/route.ts` (new), `src/app/api/logs/git/route.ts` (new), `package.json`
- **Learnings for future iterations:**
  - simple-git: use `simpleGit(projectRoot)` with named import `{ simpleGit }` from "simple-git"
  - `git.log({ maxCount: 50 })` returns `.all` array with `hash`, `message`, `date`, `author_name`
  - File not found for progress.txt returns `{ data: [], error: null }` (empty array, not 404)
  - Progress parser uses regex to extract structured data from markdown-formatted log
---

## 2026-03-02 - US-005
- Created process manager singleton in `lib/ralph-process.ts` using child_process.spawn
- POST /api/ralph/start spawns ralph.sh with tool (claude/amp) and maxIterations params
- POST /api/ralph/stop terminates running Ralph process (Windows: taskkill, Unix: SIGTERM)
- POST /api/ralph/stdin sends input string to Ralph process stdin
- GET /api/ralph/status returns status (idle/running/completed/error), iteration number, startedAt
- Duplicate start returns 409 "Ralph is already running"
- Process exit automatically cleans up reference and updates status
- Event listener pattern (`onEvent`) for future WebSocket integration
- Build and typecheck pass with zero errors
- Files changed: `src/lib/ralph-process.ts` (new), `src/app/api/ralph/start/route.ts` (new), `src/app/api/ralph/stop/route.ts` (new), `src/app/api/ralph/stdin/route.ts` (new), `src/app/api/ralph/status/route.ts` (new)
- **Learnings for future iterations:**
  - Module-level singleton state works well for process management in Next.js (persists across requests in dev)
  - `child_process.spawn` with `stdio: ["pipe", "pipe", "pipe"]` enables bidirectional communication
  - On Windows, `.sh` scripts must be spawned via `bash` command, not directly
  - `execSync("taskkill /pid X /t /f")` for Windows process termination (kills process tree)
  - Event listener pattern (onEvent/emit) enables clean WebSocket integration in US-006
  - Iteration detection via regex on stdout: `/Ralph Iteration (\d+) of (\d+)/`
---

## 2026-03-02 - US-006
- Created standalone WebSocket server on port 3001 using `ws` library in `server/ws.ts`
- Defined event types: ralph:output, ralph:status, ralph:iteration, prd:updated, progress:updated
- Client can send ralph:stdin event to forward input to Ralph process stdin
- Uses chokidar to watch prd.json and progress.txt with 500ms debounce, broadcasts prd:updated/progress:updated on change
- Ralph process stdout/stderr pipes to WebSocket via ralph-process onEvent listener (preserving ANSI sequences)
- All messages use format: `{ type: string, payload: object, timestamp: string }`
- Added `concurrently` to package.json: `npm run dev` starts both Next.js dev server and WebSocket server
- Build and typecheck pass with zero errors
- Files changed: `server/ws.ts` (new), `package.json`
- **Learnings for future iterations:**
  - WebSocket server imports ralph-process singleton directly; since it runs in a separate process via `tsx`, it has its own module state
  - Use `chokidar` with `awaitWriteFinish` for stable file change detection on Windows
  - `concurrently` with `--names` and `--prefix-colors` gives clear log output for multiple processes
  - `tsx` is needed in devDependencies to run TypeScript server files directly
  - The WS server and Next.js API routes share the same ralph-process module source but run in separate processes - for full integration, API route calls go through Next.js while real-time events flow through WebSocket
- Zustand store in `lib/store.ts` holds global state: Ralph status, iteration info, PRD data, WebSocket connection state
- WebSocket hook in `hooks/use-websocket.ts` with auto-reconnect exponential backoff (1s→30s max)
- `WebSocketProvider` component in `components/websocket-provider.tsx` wraps dashboard layout for app-level WS connection
- Derived hooks: `useRalphStatus()` and `usePrd()` read from Zustand store
- `useWebSocketEvents()` wires WS events to Zustand store updates
---

## 2026-03-02 - US-007
- Created Zustand store in `lib/store.ts` with Ralph status, PRD data, and WebSocket connection state
- Created `useWebSocket` hook with auto-reconnect (exponential backoff: 1s, 2s, 4s, 8s, 16s, max 30s)
- Created `useRalphStatus` and `usePrd` convenience hooks reading from Zustand store
- Created `useWebSocketEvents` hook that subscribes to ralph:status, ralph:iteration, and prd:updated events
- Created `WebSocketProvider` component that establishes WS connection at app level and fetches initial data
- Updated `RalphStatusIndicator` to read from Zustand store with dynamic color/animation per status
- Wired WebSocketProvider into dashboard layout
- Build and typecheck pass with zero errors
- Files changed: `src/lib/store.ts` (new), `src/hooks/use-websocket.ts` (new), `src/components/websocket-provider.tsx` (new), `src/components/ralph-status-indicator.tsx`, `src/app/(dashboard)/layout.tsx`, `package.json`
- **Learnings for future iterations:**
  - Zustand `create<T>()` with TypeScript generics provides clean type-safe global state
  - WebSocket reconnect should use `mountedRef` to prevent reconnection after component unmount
  - `useWebSocketEvents` separated from `useWebSocket` so event wiring logic is cleanly isolated
  - WebSocketProvider fetches initial Ralph status and PRD on mount, then keeps them updated via WS events
  - prd:updated event triggers a re-fetch from API rather than receiving data in the WS payload (keeps WS lean)
  - Sidebar status indicator is a client component reading Zustand store - no prop drilling needed
---

## 2026-03-02 - US-008
- Built Dashboard kanban overview page with info bar, stats row, and three-column kanban board
- Info bar shows project name, description, branch name badge, and Ralph status badge (linked to Zustand global state)
- Stats row: total stories, completed count, pending count, circular SVG progress ring with percentage
- Three-column kanban layout: Pending / In Progress / Completed with drag-and-drop via @hello-pangea/dnd
- KanbanCard component: ID badge, title, priority color left border (red/amber/zinc), passes status icon, loading spinner for in-progress
- Dropping card to Completed sets passes=true, dropping out sets passes=false (calls PUT /api/prd/stories/:id)
- Click card opens StoryEditDialog (shadcn/ui Dialog) with full edit form for title, description, criteria, notes, passes toggle
- Data auto-refreshes via WebSocket prd:updated events through Zustand store
- Installed @hello-pangea/dnd, shadcn/ui: dialog, badge, card, progress, input, textarea, select, label, switch
- Build and typecheck pass with zero errors
- Files changed: `src/app/(dashboard)/dashboard/page.tsx`, `src/components/kanban-board.tsx` (new), `src/components/kanban-card.tsx` (new), `src/components/story-edit-dialog.tsx` (new), `src/components/ui/` (8 new shadcn components), `package.json`, `package-lock.json`
- **Learnings for future iterations:**
  - @hello-pangea/dnd requires DragDropContext > Droppable > Draggable nesting; "use client" required for all DnD components
  - KanbanBoard component has `readOnly` prop to disable drag-and-drop - reuse in Archives page (US-013)
  - StoryEditDialog is a reusable component for story editing - reuse in Stories page (US-009)
  - CircularProgress SVG: use strokeDasharray/strokeDashoffset for ring progress animation
  - In Progress detection: find first story with passes=false when Ralph is running
  - Optimistic UI update pattern: update Zustand store immediately after API call, WebSocket sync as fallback
---

## 2026-03-02 - US-009
- Built user story management page with full CRUD operations
- Page header with "User Stories" title and "New Story" button
- Card list displays all stories sorted by priority, each card shows: ID badge, title, priority tag (color-coded), passes toggle switch
- "New Story" button opens shadcn/ui Sheet (slide-over panel) with form: title (required), description, acceptance criteria (dynamic add/remove rows), priority (Select: high/medium/low), notes
- Click card title opens Sheet in edit mode with all fields populated
- Delete button on each card triggers AlertDialog confirmation before deletion
- Toggle passes directly on card via Switch without opening details
- Drag-to-reorder cards using @hello-pangea/dnd with GripVertical handle (auto-calls reorder API on drop)
- All operations write to prd.json immediately and sync via WebSocket (refreshPrd reads from store)
- Installed shadcn/ui AlertDialog component
- Created StorySheet component (separate from StoryEditDialog used in kanban)
- Build and typecheck pass with zero errors
- Files changed: `src/app/(dashboard)/stories/page.tsx`, `src/components/story-sheet.tsx` (new), `src/components/ui/alert-dialog.tsx` (new)
- **Learnings for future iterations:**
  - StorySheet (Sheet-based) is distinct from StoryEditDialog (Dialog-based) - Sheet is better for detailed forms, Dialog for quick edits
  - Drag reorder uses sequential priority numbering (1, 2, 3...) assigned by array index after reorder
  - `handleDragEnd` should be wrapped in useCallback with sortedStories dependency for correct closure
  - AlertDialog from shadcn/ui works well for delete confirmation with controlled open state via `!!deleteTarget`
  - Priority label-to-number mapping: high=1-3, medium=4-7, low=8+; preserve existing number within range on edit
---
